# websockets

```sh
npm start
```

Open page http://localhost:9999

You can use following commands in browser console

for subscriptions:

```js
subscribe()
unsubscribe()
```

and for chat:

```js
message('Hi all!')
```